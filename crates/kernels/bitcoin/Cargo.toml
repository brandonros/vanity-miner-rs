[package]
name = "kernel-bitcoin"
version = "0.1.0"
edition = "2024"

# Builds bitcoin.ptx.
[lib]
name = "bitcoin"
crate-type = ["cdylib"]

[dependencies]
kernel-common = { path = "../common" }
logic = { path = "../../logic", default-features = false, features = ["bitcoin"] }
