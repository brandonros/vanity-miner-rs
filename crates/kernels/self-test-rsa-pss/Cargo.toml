[package]
name = "kernel-self-test-rsa-pss"
version = "0.1.0"
edition = "2024"

# Builds self_test_rsa_pss.ptx.
[lib]
name = "self_test_rsa_pss"
crate-type = ["cdylib"]

[dependencies]
kernel-common = { path = "../common" }
logic = { path = "../../logic", default-features = false, features = ["self_test_rsa_pss"] }
