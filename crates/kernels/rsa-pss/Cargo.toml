[package]
name = "kernel-rsa-pss"
version = "0.1.0"
edition = "2024"

# Builds rsa_pss.ptx.
[lib]
name = "rsa_pss"
crate-type = ["cdylib"]

[dependencies]
kernel-common = { path = "../common" }
logic = { path = "../../logic", default-features = false, features = ["rsa-pss"] }
