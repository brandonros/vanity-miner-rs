[package]
name = "kernel-shallenge"
version = "0.1.0"
edition = "2024"

# Builds shallenge.ptx.
[lib]
name = "shallenge"
crate-type = ["cdylib"]

[dependencies]
kernel-common = { path = "../common" }
logic = { path = "../../logic", default-features = false, features = ["shallenge"] }
