[package]
name = "kernel-rsa-modulus"
version = "0.1.0"
edition = "2024"

# Builds rsa_modulus.ptx.
[lib]
name = "rsa_modulus"
crate-type = ["cdylib"]

[dependencies]
kernel-common = { path = "../common" }
logic = { path = "../../logic", default-features = false, features = ["rsa-modulus"] }
