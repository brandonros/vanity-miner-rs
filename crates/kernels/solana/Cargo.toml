[package]
name = "kernel-solana"
version = "0.1.0"
edition = "2024"

# Builds solana.ptx.
[lib]
name = "solana"
crate-type = ["cdylib"]

[dependencies]
kernel-common = { path = "../common" }
logic = { path = "../../logic", default-features = false, features = ["solana"] }
